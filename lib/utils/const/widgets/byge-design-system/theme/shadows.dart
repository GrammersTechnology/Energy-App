import 'package:flutter/material.dart';

class AppShadows {
  static const baseShadow = BoxShadow(
    color: Color.fromRGBO(151, 158, 194, 0.25),
    blurRadius: 5.0,
    offset: Offset(0, 2),
    spreadRadius: 0,
  );
}
